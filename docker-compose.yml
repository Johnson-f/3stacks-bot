services:
  bot:
    image: johnsonf/discord-bot:latest
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      - RUST_LOG=${RUST_LOG:-info}
    # No ports exposed; the bot initiates outbound connections only

